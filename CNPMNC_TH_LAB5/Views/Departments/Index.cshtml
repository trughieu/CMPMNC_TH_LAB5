@model IEnumerable<CNPMNC_TH_LAB5.Models.Department>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DepartmentName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DepartmentName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Department1 }) |
            @Html.ActionLink("Details", "Details", new { id=item.Department1 }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Department1 })
        </td>
    </tr>
}

</table>
